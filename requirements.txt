# LocalView NLP Pipeline – Python dependencies
# Install with:  pip install -r requirements.txt
#
# Python 3.10+ recommended.

# Core data / numerics
pandas>=2.0
numpy>=1.24
scipy>=1.10

# NLP / text processing
nltk>=3.8
spacy>=3.7
textblob>=0.17
vaderSentiment>=3.3.2
wtpsplit>=2.0
transformers>=4.38
torch>=2.0           # required by transformers (CPU-only install also works)

# Topic modeling
scikit-learn>=1.3
bertopic>=0.16
hdbscan>=0.8.33
umap-learn>=0.5

# Utilities
tqdm>=4.65
pyarrow>=14.0        # for reading parquet files

# spaCy English model (run once after pip install):
#   python -m spacy download en_core_web_sm
